{
  "name": "thurs---copy",
  "version": "1.0.0",
  "description": "A fully client-side (no backend needed) web version of Creobotics: offline-style\r account creation/login, robotics learning modules across **Grade 4 through\r Grade 12 (Senior High)**, quizzes, module-locking progression, an **admin\r module-upload system**, **serial-key-gated premium access**, and progress\r tracking — all persisted in the browser's `localStorage`.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.23.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
